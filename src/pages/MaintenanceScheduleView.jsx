import MaintenanceSupervisor from './MaintenanceSupervisor';

/** Maintenance Schedule menu — same schedule list as Maintenance List (SUPERVISORAPPROVAL). */
const MaintenanceScheduleView = () => <MaintenanceSupervisor />;

export default MaintenanceScheduleView;
